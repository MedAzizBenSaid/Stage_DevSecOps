package com.studentmanagement.sms.entity;

/**
 * Type de session d'examen.
 * MAIN    -> session principale
 * CONTROL -> session de contrôle (rattrapage)
 */
public enum SessionType {
    MAIN,
    CONTROL
}
